# deps / build
node_modules/
.next/
out/
coverage/
test-results/
.playwright-mcp/
.e2e/auth.json

# codebase-RAG index (regenerated by `npm run rag:index`)
.codebase-rag/

# tsc incremental build info
*.tsbuildinfo

# env / secrets
.env
.env*.local

# misc
.DS_Store
*.log
npm-debug.log*
server

# Contract backfill fragments (transient; merged into openapi.yaml by scripts/merge-contracts.ts)
contracts/_staging/

# Service worker — generated from scripts/sw.template.js by gen-sw-version.mjs (prebuild).
# VERSION = build-time epoch ms (non-deterministic), so it must not be tracked.
public/sw.js
